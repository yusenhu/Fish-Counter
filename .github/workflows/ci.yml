name: CI

on:
  push:
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Run helper unit tests
        run: python -m unittest discover -s tests -v
      - name: Syntax check
        run: python -m py_compile fish_counter.py evaluate_models.py helpers.py
